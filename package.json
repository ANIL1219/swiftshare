{
  "name": "swiftshare",
  "version": "3.0.0",
  "description": "High-speed LAN/Hall file transfer — WebRTC P2P, no size limit",
  "main": "server.js",
  "scripts": {
    "start":    "node server.js",
    "dev":      "nodemon server.js",
    "setup":    "mkdir -p public && cp swiftshare-v3.html public/index.html && cp swiftshare-engine.js public/"
  },
  "dependencies": {
    "express":   "^4.18.2",
    "socket.io": "^4.7.2",
    "cors":      "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

